# Use edge because latest on sr.ht is currently 3.8, even though 3.9.1 is actually latest
image: alpine/edge
packages:
  - rust
sources:
  - https://git.sr.ht/~wezm/steno-lookup
tasks:
  - test: |
      cd steno-lookup
      PATH="$HOME/.cargo/bin:$PATH" cargo test
